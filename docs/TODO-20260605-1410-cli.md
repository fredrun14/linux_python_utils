# MODULE CLI — RAS (verdict GO)
> **Date :** 2026-06-05 à 14:10
> **Complexité estimée :** Faible
> **Verdict revue :** GO (aucun bloquant) — un seul point cosmétique
> **Source :** `PLAN_ACTION_REVUE.md` § cli

---

## Contexte

### Problématique
Module exemplaire : Command Pattern propre, type hints complets, docstrings FR,
couverture de tests excellente, `dry_run` conforme à l'exigence projet.
**Aucune action requise.** Un seul point cosmétique relevé.

### Fichiers impactés
- `src/linux_python_utils/cli/base.py` (documentation uniquement)

---

## Évolutions à mettre en place (Détail Junior)

### `base.py:39` — 🟡 MINEUR (documentation)
`register(self, subparsers: Any)` utilise `Any` car le type argparse
(`_SubParsersAction`) est privé. Ce choix est légitime → ajouter une note dans la
docstring expliquant pourquoi `Any` est utilisé ici :

```python
def register(self, subparsers: Any) -> None:
    """Enregistre la sous-commande.

    Args:
        subparsers: Objet retourné par ``ArgumentParser.add_subparsers``.
            Typé ``Any`` car ``argparse._SubParsersAction`` est une API
            privée non exposée par la stdlib.
    """
```

---

## Checklist d'implémentation

### Code
- [x] `base.py` — note docstring sur `subparsers: Any` (API privée stdlib)

### Tests
- [ ] Aucun nouveau test requis (couverture déjà exemplaire).

---

## Points d'attention
- Module GO : aucune urgence. Peut être traité en même temps qu'une autre PR
  touchant `cli/`, ou ignoré.

---

## ⏸ Validation requise
**Aucun code modifié avant approbation.** Répondre **"OK"** pour démarrer.
